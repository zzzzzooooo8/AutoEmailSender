{
  "name": "auto-email-sender-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev --host 127.0.0.1",
    "docs:preview": "vitepress preview --host 127.0.0.1",
    "build": "vitepress build",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4",
    "vitepress": "1.6.4",
    "vue": "^3.5.22"
  }
}
